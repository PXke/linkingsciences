# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# User's profile page
GET     /user/                           controllers.Application.user()

# About page
GET		/about/						controllers.Application.about()		

# Sign-in page
GET     /sign-in/                           controllers.Application.signIn()

# Create Project page

GET 	/createproject/				controllers.Application.createProject()

# Create Project page

GET 	/team/				controllers.Application.team()

# Create Project page

GET 	/project/				controllers.Application.project()


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)